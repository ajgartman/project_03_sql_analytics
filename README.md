SQL Analytics Tier 1 / Project 03

Hello there! My name is Andrzej and here is my SQL practice.

Description:
SQL practice repo using a synthetic manufacturing plant database. 
Built to practice JOINs, CTEs, and window functions on data with realistic messiness (NULLs, sensor dropouts, machines with no maintenance history) — not a clean tutorial dataset.

Fake data - generated by the LLM.

Tasks: 

 1.`01_oee_by_line.sql` | Which production lines hit planned output most consistently? |
 2.`02_drift_detection.sql` | Which machines show rising temperature trend vs. their own baseline? |
 3.`03_defect_rate_by_shift.sql` | Does defect rate actually differ by shift? |
 4.`04_maintenance_cost_pareto.sql` | Do a small number of machines drive most maintenance cost? |
 5.`05_maintenance_gap_vs_defects.sql` | Do longer gaps since last maintenance correlate with more defects? |
 6.`06_operator_consistency.sql` | Which operators are most consistent vs. most volatile in output accuracy? |
 7.`07_downtime_cost_by_age.sql` | Does maintenance cost rise with machine age? |
 8.`08_sensor_anomalies.sql` | Which sensor readings are outliers relative to that machine's own baseline? |
 9.`09_duration_vs_severity.sql` | Does work order duration relate to defect severity? |
 10.`10_cross_plant_comparison.sql` | How do plants compare on output accuracy and defect rate? |

## Answers

_Fill in after running each query._

1. **OEE by line:** — WITH cte AS
    (
SELECT line_id,(AVG(actual_qty)/AVG(planned_qty)) as 'ratio'
FROM work_orders
GROUP BY line_id)

SELECT *, RANK() OVER(ORDER BY ratio DESC) as 'line_ranking'
FROM cte

Used CTE to get the ratio first, then used that after to apply RANK() every row based on its ratio value. Difficulty 7/10.

2. **Drift detection:** —
3. **Defect rate by shift:** —
4. **Maintenance cost Pareto:** —
5. **Maintenance gap vs. defects:** —
6. **Operator consistency:** —
7. **Downtime cost vs. age:** —
8. **Sensor anomalies:** —
9. **Duration vs. severity:** —
10. **Cross-plant comparison:** —
